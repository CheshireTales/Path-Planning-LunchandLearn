[project]
name = "path-planning-lunchandlearn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "CheshireTales", email = "jasminelzeng@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.2",
    "numpy>=2.5.3",
    "opencv-python>=5.0.0.93",
]

[project.scripts]
path-planning-lunchandlearn = "path_planning_lunchandlearn:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
